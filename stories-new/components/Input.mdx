import { Canvas, Meta, Story, Controls } from "@storybook/blocks";
import { Template, args, parameters } from "../templates/Input/basic.js";
import "../../lib/index.js";
import { html } from "lit-html";
import * as InputStories from './Input.stories';

<Meta of={InputStories} />

# Input

Text inputs allow your users to enter letters, numbers and symbols on a single line.

<Canvas>
  <Story of={InputStories.Basic} />
</Canvas>

## API

### sgds-input

<Controls />

## Methods

### sgds-input

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>focus(options: FocusOptions)</td>
      <td>Sets focus on the input.</td>
    </tr>

    <tr>
      <td>blur()</td>
      <td>Sets blur on the input.</td>
    </tr>

    <tr>
      <td>reportValidity()</td>

      <td>
        Checks for validity and shows the browser's validation message if the
        control is invalid.
      </td>
    </tr>

    <tr>
      <td>setCustomValidity(err: string)</td>
      <td>undefined</td>
    </tr>

    <tr>
      <td>setInvalid(bool: boolean)</td>
      <td>undefined</td>
    </tr>
  </tbody>
</table>
