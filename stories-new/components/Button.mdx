import { Canvas, Meta, Story, Controls } from "@storybook/blocks";
import { Template, args, parameters } from "../templates/Button/basic.js";
import "../../lib/index.js";
import { html } from "lit-html";
import * as ButtonStories from './Button.stories';

<Meta of={ButtonStories} />

# Button

Custom button styles for actions in forms, dialogs, and more with support for multiple sizes, states, and more.

<Canvas>
  <Story of={ButtonStories.Basic} />
</Canvas>

## API

### sgds-button

<Controls />

## Methods

### sgds-button

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>focus(options: FocusOptions)</td>
      <td>Sets focus on the button.</td>
    </tr>

    <tr>
      <td>click()</td>
      <td>Simulates a click on the button.</td>
    </tr>

    <tr>
      <td>blur()</td>
      <td>Removes focus from the button.</td>
    </tr>
  </tbody>
</table>
