import { Canvas, Meta, Story, Controls } from "@storybook/blocks";
import { Template, args, parameters } from "../templates/Checkbox/basic.js";
import "../../lib/index.js";
import { html } from "lit-html";
import * as CheckboxStories from './Checkbox.stories';

<Meta of={CheckboxStories} />

# Checkbox

Checkbox component is used when you require users to select multiple items from a list.

<Canvas>
  <Story of={CheckboxStories.Basic} />
</Canvas>

## API

### sgds-checkbox

<Controls />

## Methods

### sgds-checkbox

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>click()</td>
      <td>Simulates a click on the checkbox.</td>
    </tr>

    <tr>
      <td>focus(options: FocusOptions)</td>
      <td>Sets focus on the checkbox.</td>
    </tr>

    <tr>
      <td>blur()</td>
      <td>Removes focus from the checkbox.</td>
    </tr>

    <tr>
      <td>reportValidity()</td>

      <td>
        Checks for validity and shows the browser's validation message if the
        control is invalid.
      </td>
    </tr>
  </tbody>
</table>
