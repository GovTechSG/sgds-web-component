import { Canvas, Meta, Story, Controls } from "@storybook/blocks";
import { Template, args, parameters } from "../templates/Tab/basic.js";
import "../../lib/index.js";
import { html } from "lit-html";
import * as TabStories from './Tab.stories';

<Meta of={TabStories} />

# Tab

Tab Group organizes content into a container with the syncing of tab and their corresponding panels.
Each tab must be slotted into the nav slot and its `panel` must refer to a tab panel of the same name.<br />Tab panels are used inside tab groups to display tabbed content.<br />Tabs are used within tab group to activate the tab panels

<Canvas>
  <Story of={TabStories.Basic} />
</Canvas>

## API

### sgds-tab-group

<Controls />

### sgds-tab-panel

<Controls />

### sgds-tab

<Controls />

## Methods

### sgds-tab-group

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>show(panel: string)</td>
      <td>Shows the specified tab panel.</td>
    </tr>
  </tbody>
</table>

## Methods ### sgds-tab

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>focus(options: FocusOptions)</td>
      <td>Sets focus to the tab.</td>
    </tr>

    <tr>
      <td>blur()</td>
      <td>Removes focus from the tab.</td>
    </tr>
  </tbody>
</table>
