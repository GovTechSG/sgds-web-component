import { Canvas, Meta, Story, Controls } from "@storybook/blocks";
import { Template, args, parameters } from "../templates/Stepper/basic.js";
import "../../lib/index.js";
import { html } from "lit-html";
import * as StepperStories from './Stepper.stories';

<Meta of={StepperStories} />

# Stepper

Steppers are used to inform users which step they are at in a form or a process

<Canvas>
  <Story of={StepperStories.Basic} />
</Canvas>

## API

### sgds-stepper

<Controls />

## Methods

### sgds-stepper

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>getComponent(step: number = this.activeStep)</td>

      <td>
        By default, it returns the corresponding component of the current
        activeStep as defined in the steps metadata. To get other components,
        pass in your desired step number as the parameter
      </td>
    </tr>

    <tr>
      <td>nextStep()</td>
      <td>Moves the active step forward one step</td>
    </tr>

    <tr>
      <td>previousStep()</td>
      <td>Moves the active step back one step</td>
    </tr>

    <tr>
      <td>lastStep()</td>
      <td>Changes the active step to the last step</td>
    </tr>

    <tr>
      <td>firstStep()</td>
      <td>Changes active step to the first step</td>
    </tr>

    <tr>
      <td>reset()</td>
      <td>Resets the Stepper to its initial active step state</td>
    </tr>
  </tbody>
</table>
