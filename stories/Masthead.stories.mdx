import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit";
import { withWebComponentsKnobs } from "storybook-addon-web-components-knobs";
import { withKnobs, text, number, radio } from "@storybook/addon-knobs";
import { MastheadElement } from "../lib/Masthead";

<Meta
  title="Components/Masthead"
  component={MastheadElement}
  argTypes={{
    fluid: {
      control: { type: 'boolean' }
    }
  }}
/>

export const Template = ({
  fluid
}) =>
  html`
    <masthead-element .fluid=${fluid}></masthead-element>
  `;

# MastheadElement

This is the sample file for MDX

<Canvas>
  <Story name="Basic" args={{ fluid : 'true' }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />

