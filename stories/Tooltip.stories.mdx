import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-html";
import "../lib/Tooltip";
import { partDescription, dropdownElementProps } from "./common";

<Meta
  title="Components/Tooltip"
    argTypes={{
    content: {
      control: "text",
      defaultValue: "This is a tooltip",
      table: {
        category: "props",
        type: {
          summary: "string",
        },
      },
      description: "Set the textual content of Tooltip",
    },
    placement: {
      control: "select",
      options: ["top", "bottom", "left", "right"],
      defaultValue: "top",
      table: {
        category: "props",
        type: {
          summary: "top | bottom | left | right",
        },
      },
      description:
        "Where the Tooltip is positioned relative to its target element",
    },
    offset: {
      control: "object",
      defaultValue: [0, 0],
      table: { category: "props", type: { summary: "[number, number]" } },
      description:
        "Offset of the tooltip relative to its target using a array of two numbers: [skidding, distance] ",
    },
    trigger: {
      control: "select",
      options: ["click", "hover", "focus", "hover focus"],
      defaultValue: "hover focus",
      description: "How the tooltip is triggered",
      table: {
        category: "props",
        type: {
          summary: "click | hover | focus | hover focus",
        },
      },
    },
  }}
/>

export const Template = ({content, offset, trigger, placement}) => {
  return html`
     <sgds-tooltip
      .content=${content}
      .offset=${offset}
      .trigger=${trigger}
      .placement=${placement}
    >
      <sgds-button variant="warning">Hover me</sgds-button>
    </sgds-tooltip>
  `;
};

# SgdsTooltip

<Canvas>
  <Story name="Basic">{Template.bind({})}</Story>
</Canvas>

```jsx
import {
  SgdsTooltip,
} from "@govtechsg/sgds-web-component/Tooltip";
```

<ArgsTable story="Basic" />

```jsx
<sgds-tooltip>
      <sgds-button variant="warning">Hover me</sgds-button>
    </sgds-tooltip>
```
