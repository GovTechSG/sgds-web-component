import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-html";
import "../lib/Tooltip";
import { partDescription, dropdownElementProps } from "./common";
import { commonEvents } from "./common"

<Meta
  title="Components/Tooltip"
  argTypes={{
    content: {
      control: "text",
      defaultValue: "This is a tooltip",
      table: {
        category: "props",
        type: {
          summary: "string",
        },
      },
      description: "Set the textual content of Tooltip",
    },
    placement: {
      control: "select",
      options: ["top", "bottom", "left", "right"],
      defaultValue: "top",
      table: {
        category: "props",
        type: {
          summary: "top | bottom | left | right",
        },
      },
      description:
        "Where the Tooltip is positioned relative to its target element",
    },
    trigger: {
      control: "select",
      options: ["click", "hover", "focus", "hover focus"],
      defaultValue: "hover focus",
      description: "How the tooltip is triggered",
      table: {
        category: "props",
        type: {
          summary: "click | hover | focus | hover focus",
        },
      },
    },
    ...commonEvents("SgdsTooltip")
  }}
/>

export const Template = ({ content, trigger, placement }) => {
  return html`
    <sgds-tooltip
      .content=${content}
      .trigger=${trigger}
      .placement=${placement}
    >
      <sgds-button variant="warning">Hover me</sgds-button>
    </sgds-tooltip>
  `;
};

# SgdsTooltip

`Tooltip` provides a brief description of a feature or page element.

The default `Tooltip` is hoverable, with top placement. Pass in the Tooltip text through the `content` prop as a string
The `Tooltip` component works as a wrapper around its target children element.

<Story name="Basic">{Template.bind({})}</Story>

```jsx
import { SgdsTooltip } from "@govtechsg/sgds-web-component/Tooltip";
```

<ArgsTable story="Basic" />

```jsx
<sgds-tooltip>
  <sgds-button size="sm" variant="warning">
    Hover me
  </sgds-button>
</sgds-tooltip>
```
