## Server Loading

Use `mode="server"` with `isLoading` to show the loading state while waiting for remote data.

<Canvas of={DataTableStories.ServerLoading}>
  <Story of={DataTableStories.ServerLoading} />
</Canvas>

## Header Text Alignment

Use `textAlign="right"` on `<sgds-data-table-head>` to right-align that header and all body cells in the same column.

<Canvas of={DataTableStories.HeaderTextAlignment}>
  <Story of={DataTableStories.HeaderTextAlignment} />
</Canvas>
