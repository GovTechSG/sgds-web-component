## Tone

The `tone` prop controls the visual theme of the navigation bar. There is no white/default tone — the minimum is `brand`. All tones render text and icons in fixed-light (white).

### Brand

Uses the primary surface colour as the background. This is the default.

<Canvas of={AppnavStories.ToneBrand}>
  <Story of={AppnavStories.ToneBrand} />
</Canvas>

### Gradient 1

Cyan to Purple gradient background.

<Canvas of={AppnavStories.ToneGradient1}>
  <Story of={AppnavStories.ToneGradient1} />
</Canvas>

### Gradient 2

Blue to Magenta gradient background.

<Canvas of={AppnavStories.ToneGradient2}>
  <Story of={AppnavStories.ToneGradient2} />
</Canvas>

### Gradient 3

Purple to Pink gradient background.

<Canvas of={AppnavStories.ToneGradient3}>
  <Story of={AppnavStories.ToneGradient3} />
</Canvas>

### Gradient 4

Magenta to Red gradient background.

<Canvas of={AppnavStories.ToneGradient4}>
  <Story of={AppnavStories.ToneGradient4} />
</Canvas>

## With Profile

Use `sgds-appnav-profile` in the `profile` slot for user account menus. It displays avatar + label + secondary text in desktop, and collapses into an avatar toggler with a self-contained mobile panel in smaller viewports.

- The three-dots toggler appears to the left of the profile when the default slot has items.
- In mobile, icon-buttons collapse into text menu items (using their `ariaLabel`).
- The profile has its own independent mobile panel.

<Canvas of={AppnavStories.WithProfile}>
  <Story of={AppnavStories.WithProfile} />
</Canvas>

## Profile Read-Only (No Dropdown)

When no dropdown items are slotted into `sgds-appnav-profile`, the component renders as a non-interactive, read-only display element. The dropdown menu, caret icon, and pointer cursor are all removed — the profile is not focusable or clickable.

- Use this when the profile is purely informational (e.g. showing the logged-in user without any actions).
- If items are dynamically added later, the component transitions back to its interactive dropdown state.

<Canvas of={AppnavStories.ProfileReadOnly}>
  <Story of={AppnavStories.ProfileReadOnly} />
</Canvas>
