import { Canvas, Meta, Story, ArgTypes, Markdown } from "@storybook/blocks";
import { html } from "lit";
import * as SkeletonStories from "./Skeleton.stories";

<Meta of={SkeletonStories} />

# Skeleton

A skeleton is a low-fidelity visual placeholder that represents the loading of interface elements
before they have displayed on the page.

<Canvas>
  <Story of={SkeletonStories.Basic} />
</Canvas>

## Import

### React

    ``` jsx
    import SgdsSkeleton  from "@govtechsg/sgds-web-component/react/skeleton/index.js";
    ```

### Others (Vue, Angular, plain HTML etc.)

```js
import "@govtechsg/sgds-web-component/components/Skeleton";
```

### CDN

```html
<script src="https://cdn.jsdelivr.net/npm/@govtechsg/sgds-web-component@3.16.0/components/Skeleton/index.umd.min.js"></script>
```

## API

### sgds-skeleton

<ArgTypes of="sgds-skeleton" />

## Sheen 

Add `sheen` prop to add sheening effect to the skeletons

<Canvas of={SkeletonStories.Sheen}>
  <Story of={SkeletonStories.Sheen} />
</Canvas>

## Rows

To skeletonised paragraph text, set a number to `rows` prop. 
As the gap between the rows are fixed to `--sgds-gap-xs`, the rows will auto-sized (appear thicker or thinner) depending on the `height` of the skeleton.

<Canvas of={SkeletonStories.Rows}>
  <Story of={SkeletonStories.Rows} />
</Canvas>