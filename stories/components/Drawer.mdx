import { Canvas, Meta, Story, ArgTypes, Markdown } from "@storybook/blocks";
import { html } from "lit";
import * as DrawerStories from "./Drawer.stories";

<Meta of={DrawerStories} />

# Drawer

Drawers slide in from a container to expose additional options and information.

<Canvas>
  <Story of={DrawerStories.Basic} />
</Canvas>

## Import

### React

    ``` jsx
    import SgdsDrawer  from "@govtechsg/sgds-web-component/react/drawer/index.js";
    ```

### Others (Vue, Angular, plain HTML etc.)

```js
import "@govtechsg/sgds-web-component/components/Drawer";
```

### CDN

```html
<script src="https://cdn.jsdelivr.net/npm/@govtechsg/sgds-web-component@3.16.0/components/Drawer/index.umd.min.js"></script>
```

## API

### sgds-drawer

<ArgTypes of="sgds-drawer" />

## Methods

### sgds-drawer

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>show()</td>
      <td>Shows the drawer.</td>
    </tr>
    <tr>
      <td>hide()</td>
      <td>Hides the drawer</td>
    </tr>
  </tbody>
</table>

## Placement

The drawer supports multiple placement options to control where the drawer appears on the screen.

By default, the drawer opens from the right (end). You can specify different placements using the placement attribute:

- `placement="start"`: Opens from the left
- `placement="end"`: Opens from the right
- `placement="top"`: Slides in from the top
- `placement="bottom"`: Slides in from the bottom

<Canvas of={DrawerStories.Placement}>
  <Story of={DrawerStories.Placement} />
</Canvas>

## Sizes

The drawer supports different sizes for its panel. This controls the **width** (when placed at start/end) or the **height** (when placed at top/bottom).

You can specify the size using the `size` attribute:

- `size="sm"`: Small
- `size="md"`: Medium
- `size="lg"`: Large

<Canvas of={DrawerStories.Sizes}>
  <Story of={DrawerStories.Sizes} />
</Canvas>