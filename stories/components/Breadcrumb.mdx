import { Canvas, Meta, Story, ArgTypes, Markdown } from "@storybook/blocks";
import { html } from "lit";
import * as BreadcrumbStories from "./Breadcrumb.stories";

<Meta of={BreadcrumbStories} />

# Breadcrumb

Breadcrumb Item are navigational links used in Breadcrumb component<br/>Breadcrumbs help users to navigate and understand where they are on the current website or service.

<Canvas>
  <Story of={BreadcrumbStories.Basic} />
</Canvas>

## Import

### React

    ``` jsx
    import SgdsBreadcrumbItem  from "@govtechsg/sgds-web-component/react/breadcrumb-item/index.js";
    ```

    ``` jsx
    import SgdsBreadcrumb  from "@govtechsg/sgds-web-component/react/breadcrumb/index.js";
    ```

### Others (Vue, Angular, plain HTML etc.)

```js
import "@govtechsg/sgds-web-component/components/Breadcrumb";
```

### CDN

```html
<script src="https://cdn.jsdelivr.net/npm/@govtechsg/sgds-web-component@3.16.0/components/Breadcrumb/index.umd.min.js"></script>
```

## API

### sgds-breadcrumb-item

<ArgTypes of="sgds-breadcrumb-item" />

### sgds-breadcrumb

<ArgTypes of="sgds-breadcrumb" />

## Pass in anchor tags in default slot of `sgds-breadcrumb-item`

For better SEO, anchor tags are to be passed by the user in the light dom or the slot of `sgds-breadcrumb-item`. 
Every `sgds-breadcrumb-item` requires an anchor element. 

## Overflow menu 

Breadcrumb should show up to 4 breadcrumb items. Beyond that, the breadcrumb component will be re-grouped the excess items and render in a overflow dropdown menu automatically, displaying only the first , second last and last links 


<Canvas of={BreadcrumbStories.Overflow}>
  <Story of={BreadcrumbStories.Overflow} />
</Canvas>