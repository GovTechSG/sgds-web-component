import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-html";
import "../../lib/Toast";
import "../../lib/CloseButton";

<Meta
  title='Components/Toast'
  argTypes={{
    bg: {
      control: "select",
      options: [
        "primary",
        "secondary",
        "success",
        "danger",
        "warning",
        "info",
        "dark",
        "light",
      ],
      table: { category: "props", type: { summary: "string" } },
      description: "The toast bg visual color variant",
    },
    variant: {
      control: "select",
      options: [
        "primary",
        "secondary",
        "success",
        "danger",
        "warning",
        "info",
        "dark",
        "light",
      ],
      table: { category: "props", type: { summary: "string" } },
      description: "The toast bg visual color variant",
    },
    status: {
      control: "select",
      options: ["success", "danger", "warning"],
      table: { category: "props", type: { summary: "string" } },
      description:
        "Adds CSS styling to `<Toast />` based on the defined status",
    },
    show: {
      control: "boolean",
      defaultValue: true,
      table: { category: "props", type: { summary: "boolean" } },
      description: "Controls the visual state of the Toast",
    },
    closeLabel: {
      control: "text",
      table: { category: "props", type: { summary: "string" } },
      description: "Sets the aria-label for toast close button",
    },
    autohide: {
      control: "boolean",
      table: { category: "props", type: { summary: "boolean" } },
      description: "sets to true to enable autohide",
    },
    delay: {
      control: "text",
      table: { category: "props", type: { summary: "number" } },
      description: "Sets the duration for autohide eg. 5000 ms",
    },
    "sgds-close": {
      description: "Emitted event when dismissible button is clicked",
      table: {
        category: `Event: SgdsToast`,
      },
    },
  }}
/>

export const Template = (args) =>
  html`
    <sgds-toast
      .show=${args.show}
      .bg=${args.bg}
      .variant=${args.variant}
      .status=${args.status}
      .closeLabel=${args.closeLabel}
      .autohide=${args.autohide}
      .delay=${args.delay}
    ></sgds-toast>
  `;

# Toast

## `<sgds-toast>`

## Basic

Provide contextual feedback messages for typical user actions with the handful of available and flexible alert messages.

<Canvas>
  <Story name='Basic'>{Template.bind({})}</Story>
</Canvas>

## API

<ArgsTable story='Basic' />
