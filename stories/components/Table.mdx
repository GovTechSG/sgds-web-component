import { Canvas, Meta, Story, ArgTypes, Markdown } from "@storybook/blocks";
import { html } from "lit";
import * as TableStories from "./Table.stories";

<Meta of={TableStories} />

# Table

Table cell is used for presenting individual data values, interactive elements, or custom content within a table row.<br/>Table head represents a table header cell that identifies a group of information within the table.<br/>Table row organizes and groups table cells or header cells into a single horizontal line within the table.<br/>Table is used for displaying collections of data in organized rows and columns.
It supports two rendering methods: supply an array of data for automatic table generation, or use the slot to insert custom table elements for full structural control.

<Canvas>
  <Story of={TableStories.Basic} />
</Canvas>

## Import

### React

    ``` jsx
    import SgdsTableCell  from "@govtechsg/sgds-web-component/react/table-cell/index.js";
    ```

    ``` jsx
    import SgdsTableHead  from "@govtechsg/sgds-web-component/react/table-head/index.js";
    ```

    ``` jsx
    import SgdsTableRow  from "@govtechsg/sgds-web-component/react/table-row/index.js";
    ```

    ``` jsx
    import SgdsTable  from "@govtechsg/sgds-web-component/react/table/index.js";
    ```

### Others (Vue, Angular, plain HTML etc.)

```js
import "@govtechsg/sgds-web-component/components/Table";
```

### CDN

```html
<script src="https://cdn.jsdelivr.net/npm/@govtechsg/sgds-web-component@3.16.0/components/Table/index.umd.min.js"></script>
```

## API

### sgds-table-cell

<ArgTypes of="sgds-table-cell" />

### sgds-table-head

<ArgTypes of="sgds-table-head" />

### sgds-table-row

<ArgTypes of="sgds-table-row" />

### sgds-table

<ArgTypes of="sgds-table" />

## Responsive

Responsive tables allow tables to be scrolled horizontally with ease.

## Always Responsive

Across every breakpoint, use `responsive="always"` for horizontally scrolling tables. Responsive tables are wrapped automatically in a `div`. The following example has 12 columns that are scrollable horizontally.

<Canvas of={TableStories.AlwaysResponsive}>
  <Story of={TableStories.AlwaysResponsive} />
</Canvas>

## Breakpoint Specific

Use `responsive="sm"`, `responsive="md"` , `responsive="lg"`, or `responsive="xl"` as needed to create responsive tables up to a particular breakpoint. From that breakpoint and up, the table will behave normally and not scroll horizontally.

<Canvas of={TableStories.Responsive}>
  <Story of={TableStories.Responsive} />
</Canvas>

## Structured Elements

Use `sgds-table-row`, `sgds-table-cell`, `sgds-table-head` to build the whole table

Example with horizontal header

<Canvas of={TableStories.StructuredElements}>
  <Story of={TableStories.StructuredElements} />
</Canvas>

Example with vertical header

<Canvas of={TableStories.StructuredElementsVertical}>
  <Story of={TableStories.StructuredElementsVertical} />
</Canvas>

Example with both header

<Canvas of={TableStories.StructuredElementsBoth}>
  <Story of={TableStories.StructuredElementsBoth} />
</Canvas>
