import { Canvas, Meta, Story, ArgTypes, Markdown } from "@storybook/blocks";
import { html } from "lit";
import * as StepperStories from "./Stepper.stories";

<Meta of={StepperStories} />

# Stepper

Steppers are used to inform users which step they are at in a form or a process

<Canvas>
  <Story of={StepperStories.Basic} />
</Canvas>

## Import

### React

    ``` jsx
    import SgdsStepper  from "@govtechsg/sgds-web-component/react/stepper/index.js";
    ```

### Others (Vue, Angular, plain HTML etc.)

```js
import "@govtechsg/sgds-web-component/components/Stepper";
```

### CDN

```html
<script src="https://cdn.jsdelivr.net/npm/@govtechsg/sgds-web-component@3.16.0/components/Stepper/index.umd.min.js"></script>
```

## API

### sgds-stepper

<ArgTypes of="sgds-stepper" />

## Methods

### sgds-stepper

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>getComponent(step: number = this.activeStep)</td>
      <td>
        By default, it returns the corresponding component of the current
        activeStep as defined in the steps metadata. To get other components,
        pass in your desired step number as the parameter
      </td>
    </tr>
    <tr>
      <td>nextStep()</td>
      <td>Moves the active step forward one step</td>
    </tr>
    <tr>
      <td>previousStep()</td>
      <td>Moves the active step back one step</td>
    </tr>
    <tr>
      <td>lastStep()</td>
      <td>Changes the active step to the last step</td>
    </tr>
    <tr>
      <td>firstStep()</td>
      <td>Changes active step to the first step</td>
    </tr>
    <tr>
      <td>reset()</td>
      <td>Resets the Stepper to its initial active step state</td>
    </tr>
  </tbody>
</table>

## Pass IStepsMetadata to steps prop 

`steps` prop is an array of object defined by interface `IStepMetaData`

```tsx
export interface IStepMetaData {
  component: unknown;
  stepHeader: string;
}
```

- `component` refers to the component to render for each step. The type of component set to unknown as it depends on which frontend library you are using. 
For example, it could be a vue component / react component / angular component depending on your frontend library of choice
- `stepHeader` refers to the header title per step.
- The step number is defined by the object's index of the array

## Vertical stepper

By default, stepper is of `horizontal` orientation. Set stepper to vertical with `orientation="vertical"`

<Canvas of={StepperStories.Orientation}>
  <Story of={StepperStories.Orientation} />
</Canvas>

## Clickable steps

By default steps indicator are non-interactive. To enable steps to be clickable, add `clickable` prop. 

    Note that the steps are only backwards clickable. 
    To allow users to go back and forth, please create the action buttons with the methods from the methods table above.


<Canvas of={StepperStories.Clickable}>
  <Story of={StepperStories.Clickable} />
</Canvas>

## Stepper in a form context

The example below is build with Lit library. As each frontend library has their own lifecycle hooks and unique syntax for props and event handling, 
you will need to manage the rendering of components on your end. Here is a general guide to building a form stepper like the one below. 
Reference the API table and methods table carefully when creating the Stepper component.

1. Build your child components. The stepper example has three steps and each step has its own component.
  These three components will be the direct child of your StepperComponent : 

  - Step 1: Personal Details 
  - Step 2: Address and Contact details 
  - Step 3: Review  

2. Create the parent component. This is where all the state of `steps` and `activeStep` will be managed and should persist. 
  This component should capture and store any user input. This component should handle the dynamic rendering of child components depending on the current `activeStep`.
  Use SgdsStepper's  `getComponent` public method to obtain current component.
  Make use of the SgdsStepper's public methods to create button actions for next/previous/last/first step or reset. 


<Canvas of={StepperStories.StepperExample}>
  <Story of={StepperStories.StepperExample} />
</Canvas>

## Working Examples

### React 

Please see a <a target="_blank" href="https://stackblitz.com/github/clukhei/sgds-wc-react-stepper?file=src%2FApp.tsx">working example of stepper on an React app</a>

### Angular

Please see a <a target="_blank" href="https://stackblitz.com/github/clukhei/angular-stepper">working example of stepper on an Angular app</a>

### Vue 

Please see a <a target="_blank" href="https://stackblitz.com/github/tonghauhive/sgds-vue-stepper?file=src%2Fcomponents%2FStepper.vue">working example of stepper on an Vue app</a>


## Contributions

We accept contributions from the community! 
If you have an SgdsStepper to showcase from a different framework or a better example to showcase
please raise a PR or raise an issue with your stackblitz example on our <a href="https://github.com/GovTechSG/sgds-web-component" target="_blank">github page</a>.
If the examples above are not working, please also  notify us by raising a github issue. 