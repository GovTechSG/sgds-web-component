import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit";
import { withWebComponentsKnobs } from "storybook-addon-web-components-knobs";
import { withKnobs, text, number } from "@storybook/addon-knobs";
import "../lib/Checkbox";
import { SgdsCheckbox } from "../lib/Checkbox";

<Meta
  title='Components/Checkbox'
  //handles format: ['eventName', 'selector']
  parameters={{
    actions: {
      handles: ["sgds-checkbox", "sgds-checkbox"],
    },
  }}
  argTypes={{
    value: {
      control: "text",
      table: {
        category: "props",
        type: {
          summary: "string",
        },
      },
      description:
        "Value of the HTML form control. Primarily used to differentiate a list of related checkboxes that have the same name.",
    },
    id: {
      control: "text",
      description: "For label/input value pair",
      table: {
        category: "props",
        type: {
          summary: "string",
        },
      },
    },
    name: {
      control: "text",
      table: {
        category: "props",
        type: {
          summary: "string",
        },
      },
      description:
        "Name of the HTML form control. Submitted with the form as part of a name/value pair.",
    },
    disabled: {
      control: "boolean",
      defaultValue: false,
      description:
        "Disables the checkbox '(so the user can't check / uncheck it)'",
      table: {
        category: "props",
        defaultValue: { summary: false },
        type: {
          summary: "boolean",
        },
      },
    },
    required: {
      control: "boolean",
      defaultValue: false,
      description: "Makes the checkbox a required field",
      table: {
        category: "props",
        defaultValue: { summary: false },
        type: {
          summary: "boolean",
        },
      },
    },
    checked: {
      control: "boolean",
      defaultValue: false,
      description:
        "Draws the checkbox in a checked state, which toggles input aria-checked to true",
      table: {
        category: "props",
        defaultValue: { summary: false },
        type: {
          summary: "boolean",
        },
      },
    },
    isValid: {
      control: "boolean",
      defaultValue: false,
      description:
        "Manually style the input as valid. If showFeedback is true, styles feedback div element as well",
      table: {
        category: "props",
        defaultValue: { summary: false },
        type: {
          summary: "boolean",
        },
      },
    },
    isInvalid: {
      control: "boolean",
      defaultValue: false,
      description:
        "Manually style the input as invalid. if showFeedback is true, styles feedback div element as well",
      table: {
        category: "props",
        defaultValue: { summary: false },
        type: {
          summary: "boolean",
        },
      },
    },
    showFeedback: {
      control: "boolean",
      defaultValue: false,
      description: "Manually render feedback element",
      table: {
        category: "props",
        defaultValue: { summary: false },
        type: {
          summary: "boolean",
        },
      },
    },
    defaultSlot: {
      control: "text",
      defaultValue: "Default text here",
      description: "default slot for label element",
      table: {
        category: "slots",
        type: {
          summary: "string",
        },
      },
    },
    feedbackSlot: {
      control: "text",
      defaultValue: "Feedback text here",
      description: "feedback slot for div element",
      table: {
        category: "slots",
        type: {
          summary: "string",
        },
      },
    },
    customEvent: {
      name: "sgds-checkbox",
      control: "disable",
      defaultValue: "Feedback text here",
      description:
        "Emits a custom event on click. Refer to canvas mode to view event handler in action.",
      table: {
        category: "events",
      },
    },
  }}
/>

export const Template = (args) => {
  return html`
    <sgds-checkbox
      .name=${args.name}
      ?disabled=${args.disabled}
      value=${args.value}
      ?required=${args.required}
      .checked=${args.checked}
      .isInvalid=${args.isInvalid}
      .isValid=${args.isValid}
      .id=${args.id}
      .showFeedback=${args.showFeedback}
      >${args.defaultSlot}<div slot="feedback">${args.feedbackSlot}<div></sgds-checkbox
    >
  `;
};

# Checkbox

## Basic

<Canvas>
  <Story name='Basic'>{Template.bind({})}</Story>
</Canvas>

## API

```jsx
import { SgdsCheckbox } from "@govtechsg/sgds-web-component/Checkbox";
```

<ArgsTable story='Basic' />
