import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-html";
import "../lib/Breadcrumb";

<Meta
  title="Components/Breadcrumb"
  argTypes={{
    breadcrumbClasses: { 
      control: "text" ,
      table: { 
        category: "sgds-breadcrumb" 
        },
      description: 'Optional. Can be used to insert any utility classes such as `me-auto`'
    },
    href:{
      control:"text",
      defaultValue: undefined,
      table: { 
        category: "sgds-breadcrumb-item",
        type: { summary: "Attribute"}
        },
      description:'When href is set, the breadcrumb item will be rendered as an `<a>` with this `href` instead of a `<div>`.'
    },
    
    target:{
      control: 'select',
      options:[
        '_self',
        '_blank',
        '_parent',
        '_top'
      ],
      defaultValue: '',
      table: { 
        category: "sgds-breadcrumb-item",
        type: { summary: "Attribute"}
        },
      description:'Tells the browser where to open the link. Only used when `href` is set.'
    },
    rel:{
      defaultValue: "noreferrer noopener",
      table: { 
        category: "sgds-breadcrumb-item",
        type: { summary: "Attribute"}
        },
      description:'When target is set, rel="noreferrer noopener" attribute will be added to the breadcrumb-item'
    }
  }}
/>

export const Template = (args) =>
  html`
    <sgds-breadcrumb 
        .breadcrumbClasses=${args.breadcrumbClasses} 
    >
        <sgds-breadcrumb-item href="https://www.yahoo.com">Home</sgds-breadcrumb-item>
        <sgds-breadcrumb-item href="https://www.google.com">Item 1</sgds-breadcrumb-item>
        <sgds-breadcrumb-item href="https://www.google.com">Item 2</sgds-breadcrumb-item>
        <sgds-breadcrumb-item href="https://www.google.com">Item 3</sgds-breadcrumb-item>
        <sgds-breadcrumb-item href="https://www.google.com">Last Item</sgds-breadcrumb-item>
    </sgds-breadcrumb>
  `;

# SgdsBreadcrumb

Breadcrumbs help users to navigate and understand where they are on the current website or service.

<Canvas>
  <Story name="Basic">
    {Template.bind({})}
  </Story>
</Canvas>

## API

```jsx
import { SgdsBreadcrumb } from "@govtechsg/sgds-web-component";
```

<ArgsTable story="Basic" />