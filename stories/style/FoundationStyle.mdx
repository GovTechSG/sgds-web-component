import { Meta, Markdown, Canvas, Story } from "@storybook/blocks";
import FoundationalStyles from "../../docs/FoundationalStyles.md?raw";
import * as FoundationStories from "./foundation.stories.js";

<Meta of={FoundationStories} />

<Markdown>{FoundationalStyles}</Markdown>

## Technical Reference

Learn how to use the CSS variables that power the responsive typography and layout system:

### Breakpoints

The responsive system uses three primary breakpoints:

- **Mobile (Base)**: 0px and above (default)
- **Tablet**: 1024px and above
- **Desktop**: 1440px and above

### Typography Variables

Font sizes for headings, body text, labels, and captions automatically scale at different breakpoints:

#### Headings

- `--sgds-font-size-heading-sm`: Responsive H4 sizing
- `--sgds-font-size-heading-md`: Responsive H3 sizing
- `--sgds-font-size-heading-lg`: Responsive H2 sizing
- `--sgds-font-size-heading-xl`: Responsive H1 sizing

#### Body Text

- `--sgds-font-size-body-sm`: Small body text (14px on all viewports)
- `--sgds-font-size-body-md`: Regular body text (16px on all viewports)
- `--sgds-font-size-body-lg`: Large body text (responsive: 18px mobile → 20px tablet/desktop)

#### Line Heights

Line height variables scale proportionally with font sizes for improved readability:

- `--sgds-line-height-xs` through `--sgds-line-height-3-xl`

Example progression:

- Mobile: `--sgds-line-height-xs: 24px`, `--sgds-line-height-md: 28px`
- Tablet (1024px+): `--sgds-line-height-xs: 24px`, `--sgds-line-height-md: 32px`
- Desktop (1440px+): `--sgds-line-height-xs: 24px`, `--sgds-line-height-md: 32px`

### Layout and Spacing Variables

These variables enable responsive spacing and layout adjustments:

#### Text Gaps

Control spacing between text elements: `--sgds-text-gap-2-xs` through `--sgds-text-gap-2-xl`

#### Layout Gaps

Control spacing in layout components:

- Mobile: `--sgds-layout-gap-md: 1rem`
- Tablet (1024px+): `--sgds-layout-gap-md: 1.5rem`
- Desktop (1440px+): `--sgds-layout-gap-md: 2rem`

#### Layout Padding

Responsive padding for containers:

- Mobile: `--sgds-layout-padding-md: 1.5rem`
- Tablet (1024px+): `--sgds-layout-padding-md: 2rem`
- Desktop (1440px+): `--sgds-layout-padding-md: 3rem`

#### Component Spacing

Consistent spacing for UI components that adapts to screen size:

- `--sgds-component-gap-xs` through `--sgds-component-gap-xl`
- `--sgds-component-padding-xs` through `--sgds-component-padding-xl`

#### Container Width

The container width automatically adjusts for optimal content display:

- Mobile: `--sgds-container-width: 360px`
- Tablet (1024px+): `--sgds-container-width: 888px`
- Desktop (1440px+): `--sgds-container-width: 1312px`

## Headings

Headings (h1–h6) automatically use responsive typography variables that scale at each breakpoint. Font sizes, line heights, letter spacing, and margins all adjust for optimal readability across devices.

**Responsive heading sizing:**

- **h1**: 32px (mobile) → 36px (tablet) → 40px (desktop)
- **h2**: 28px (mobile) → 30px (tablet) → 32px (desktop)
- **h3**: 24px (mobile) → 26px (tablet) → 28px (desktop)
- **h4**: 20px (mobile) → 22px (tablet) → 24px (desktop)
- **h5** and **h6**: Scale with subtitle sizing variables

Each heading uses appropriate font weight, letter spacing, and line height to maintain visual hierarchy and readability at every breakpoint.

<Canvas of={FoundationStories.Headings}>
  <Story of={FoundationStories.Headings} />
</Canvas>

## Paragraph

Paragraphs use responsive body typography variables for consistent, readable text across all screen sizes. The default body text (`--sgds-font-size-body-md`) scales appropriately at each breakpoint, with consistent line heights and spacing.

**Paragraph styling:**

- Font size: Uses responsive body variables (default 16px mobile/desktop)
- Line height: 24px (xs) for optimal text readability
- Margin bottom: 2rem spacing between paragraphs
- Letter spacing: Normal for standard readability

<Canvas of={FoundationStories.Paragraph}>
  <Story of={FoundationStories.Paragraph} />
</Canvas>

## Anchor

<Canvas of={FoundationStories.Anchor}>
  <Story of={FoundationStories.Anchor} />
</Canvas>

Links inherit responsive typography from their parent elements and include built-in visual states for better user interaction:

- Default: Underlined with default link color
- Hover/Focus: Emphasis color for visual feedback
- External links: Automatically display an external link icon for clarity

## Caption

<Canvas of={FoundationStories.Caption}>
  <Story of={FoundationStories.Caption} />
</Canvas>

Captions use semantic color and typography variables for consistent styling on table captions and other caption elements:

- Font size: 14px (caption-md)
- Font weight: Regular (400)
- Line height: 20px (2-xs) for compact readability
- Color: Subtle label color for visual de-emphasis

## Label

<Canvas of={FoundationStories.Label}>
  <Story of={FoundationStories.Label} />
</Canvas>

Labels use responsive body typography variables to maintain consistency with form elements:

- Font size: Responsive body-md sizing
- Font weight: Regular (400)
- Line height: 24px (xs)
- Color: Default label color

## List

### Unordered

<Canvas of={FoundationStories.ULists}>
  <Story of={FoundationStories.ULists} />
</Canvas>

### Ordered

<Canvas of={FoundationStories.OLists}>
  <Story of={FoundationStories.OLists} />
</Canvas>

Lists (`<ol>`, `<ul>`, `<li>`) use responsive body typography with semantic spacing:

- Font size: Responsive body-md sizing
- Font weight: Regular (400)
- Line height: 24px (xs) for readability
- Margin/padding: Uses responsive spacing variables for consistent layout
- Default padding-left: 2rem
- Item margin-top: 0.5rem spacing between items

## CSS Variables Reference

### How Responsive Variables Work

All typography and layout variables are defined at the root level with default (mobile) values, then updated via media queries at each breakpoint. This approach ensures:

1. **Progressive enhancement**: Mobile styles load first, then enhance on larger screens
2. **Consistency**: All components using the same variables stay aligned
3. **Flexibility**: Override defaults by adjusting CSS variables in your own code

Example variable definition:

```css
:root {
  --sgds-font-size-heading-xl: var(--sgds-font-size-32); /* Mobile: 32px */
  --sgds-line-height-xl: var(--sgds-line-height-40);
}

@media screen and (min-width: 1024px) {
  :root {
    --sgds-font-size-heading-xl: var(--sgds-font-size-36); /* Tablet: 36px */
    --sgds-line-height-xl: var(--sgds-line-height-44);
  }
}

@media screen and (min-width: 1440px) {
  :root {
    --sgds-font-size-heading-xl: var(--sgds-font-size-40); /* Desktop: 40px */
    --sgds-line-height-xl: var(--sgds-line-height-48);
  }
}
```

### Source files

<iframe
  src="https://gist.github.com/clukhei/a31bfc0ea0c2b87d950b125f92835a76.pibb"
  style={{ width: "100%", height: "1000px", border: 0 }}
></iframe>
