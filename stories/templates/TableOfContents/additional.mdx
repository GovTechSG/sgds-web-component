## Pass in anchor tags to the `contents` slot

For better SEO, anchor tags are to be passed by the user in the light dom. It is recommended to use `sgds-link` to wrap the anchor tags.
Every `contents` slot requires an anchor element.