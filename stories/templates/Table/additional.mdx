
## Sorting

Sorting on a column is enabled by adding the `sort` property. The sorting algorithm is based on javascript array.sort() method. In ascending order from bottom, alphabets come first, followed by numbers, and then symbols. Similarly, in descending order from bottom, symbols come first, followed by numbers, and then alphabets.

export const sortTemplate = (
  { 
    sort
  }
) =>
  html` 
  <sgds-table 
    sort=${sort}
    tableHeaders='["Serial No.", "Title", "Name", "Username"]'
    tableData='[
        ["S901021", "David", "Doe", "@johndoe"],
        ["S892002", "2be", "Zoe", "@janedoe"],
        ["S879192", "#55", "Smith", "@bobsmith"],
        ["S829132", "@hey", "Alex", "@davidsmith"]
      ]'
> 
  </sgds-table>
  `;

<Canvas>
  <Story name="Sorting">
    {sortTemplate({
      sort: true
    })}
  </Story>
</Canvas>

## Removable Sort

When `removableSort` is present, the third click removes the sorting from the column.

export const removableSortTemplate = (
  { 
    removableSort
  }
) =>
  html` 
  <sgds-table 
    removableSort=${removableSort}
    tableHeaders='["Serial No.", "Title", "Name", "Username"]'
    tableData='[
        ["S901021", "David", "Doe", "@johndoe"],
        ["S892002", "2be", "Zoe", "@janedoe"],
        ["S879192", "#55", "Smith", "@bobsmith"],
        ["S829132", "@hey", "Alex", "@davidsmith"]
      ]'
> 
  </sgds-table>
  `;

<Canvas>
  <Story name="removableSort">
    {removableSortTemplate({
      removableSort: true
    })}
  </Story>
</Canvas>

## Striped Body

Adds zebra-striping using striped to table row within the `<tbody>`.

export const stripedTemplate = (
  { 
    striped,
  }
) =>
  html` 
  <sgds-table 
    striped=${striped}
    tableHeaders='["Serial No.", "Title", "Name", "Username"]'
    tableData='[
        ["S901021", "David", "Doe", "@johndoe"],
        ["S892002", "2be", "Zoe", "@janedoe"],
        ["S879192", "#55", "Smith", "@bobsmith"],
        ["S829132", "@hey", "Alex", "@davidsmith"]
      ]'
> 
  </sgds-table>
  `;

<Canvas>
  <Story name="striped">
    {stripedTemplate({
      striped: true
    })}
  </Story>
</Canvas>

## Bordered Body

Use `bordered` to add borders on all sides of the table and cells.

export const borderedTemplate = (
  { 
    bordered,
  }
) =>
  html` 
  <sgds-table 
    bordered=${bordered}
    tableHeaders='["Serial No.", "Title", "Name", "Username"]'
    tableData='[
        ["S901021", "David", "Doe", "@johndoe"],
        ["S892002", "2be", "Zoe", "@janedoe"],
        ["S879192", "#55", "Smith", "@bobsmith"],
        ["S829132", "@hey", "Alex", "@davidsmith"]
      ]'
> 
  </sgds-table>
  `;

<Canvas>
  <Story name="bordered">
    {borderedTemplate({
      bordered: true
    })}
  </Story>
</Canvas>

## Borderless Body

Add `borderless` to remove all borders on the table and cells, including table header.

export const borderlessTemplate = (
  { 
    borderless,
  }
) =>
  html` 
  <sgds-table 
    borderless=${borderless}
    tableHeaders='["Serial No.", "Title", "Name", "Username"]'
    tableData='[
        ["S901021", "David", "Doe", "@johndoe"],
        ["S892002", "2be", "Zoe", "@janedoe"],
        ["S879192", "#55", "Smith", "@bobsmith"],
        ["S829132", "@hey", "Alex", "@davidsmith"]
      ]'
> 
  </sgds-table>
  `;

<Canvas>
  <Story name="borderless">
    {borderlessTemplate({
      borderless: true
    })}
  </Story>
</Canvas>

## Hovered Body

Use hover to enable a hover state on table rows within a `<tbody>`.

export const hoverTemplate = (
  { 
    hover,
  }
) =>
  html` 
  <sgds-table 
    hover=${hover}
    tableHeaders='["Serial No.", "Title", "Name", "Username"]'
    tableData='[
        ["S901021", "David", "Doe", "@johndoe"],
        ["S892002", "2be", "Zoe", "@janedoe"],
        ["S879192", "#55", "Smith", "@bobsmith"],
        ["S829132", "@hey", "Alex", "@davidsmith"]
      ]'
> 
  </sgds-table>
  `;

<Canvas>
  <Story name="hover">
    {hoverTemplate({
      hover: true
    })}
  </Story>
</Canvas>

## Dark Table

Use `variant="dark"` to invert the colors of the table and get light text on a dark background.

export const variantTemplate = (
  { 
    variant,
  }
) =>
  html` 
  <sgds-table 
    variant=${variant}
    tableHeaders='["Serial No.", "Title", "Name", "Username"]'
    tableData='[
        ["S901021", "David", "Doe", "@johndoe"],
        ["S892002", "2be", "Zoe", "@janedoe"],
        ["S879192", "#55", "Smith", "@bobsmith"],
        ["S829132", "@hey", "Alex", "@davidsmith"]
      ]'
> 
  </sgds-table>
  `;

<Canvas>
  <Story name="variant">
    {variantTemplate({
      variant: "dark"
    })}
  </Story>
</Canvas>


## Responsive

Responsive tables allow tables to be scrolled horizontally with ease.

## Always Responsive

Across every breakpoint, use `responsive` for horizontally scrolling tables. Responsive tables are wrapped automatically in a `div`. The following example has 12 columns that are scrollable horizontally.

export const alwaysResponsiveTemplate = (
  { 
    alwaysResponsive,
  }
) =>
  html` 
  <sgds-table 
    alwaysResponsive=${alwaysResponsive}
    tableHeaders='["Serial No.", "Title", "Name", "Username"]'
    tableData='[
        ["S901021", "David", "Doe", "@johndoe"],
        ["S892002", "2be", "Zoe", "@janedoe"],
        ["S879192", "#55", "Smith", "@bobsmith"],
        ["S829132", "@hey", "Alex", "@davidsmith"]
      ]'
> 
  </sgds-table>
  `;

<Canvas>
  <Story name="alwaysResponsive">
    {alwaysResponsiveTemplate({
      alwaysResponsive: true
    })}
  </Story>
</Canvas>

## Breakpoint Specific

Use `responsive="sm"`, `responsive="md"` , `responsive="lg"`, or `responsive="xl"` as needed to create responsive tables up to a particular breakpoint. From that breakpoint and up, the table will behave normally and not scroll horizontally.

export const responsiveTemplate = (
  { 
    responsive,
  }
) =>
  html` 
  <sgds-table 
    responsive=${responsive}
    tableHeaders='["Serial No.", "Title", "Name", "Username"]'
    tableData='[
        ["S901021", "David", "Doe", "@johndoe"],
        ["S892002", "2be", "Zoe", "@janedoe"],
        ["S879192", "#55", "Smith", "@bobsmith"],
        ["S829132", "@hey", "Alex", "@davidsmith"]
      ]'
> 
  </sgds-table>
  `;

<Canvas>
  <Story name="responsive">
    {responsiveTemplate({
      responsive: "sm"
    })}
  </Story>
</Canvas>