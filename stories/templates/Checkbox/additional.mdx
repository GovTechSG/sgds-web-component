## Disabled

Set checkbox to disabled state using `disabled` prop

<Canvas of={CheckboxStories.Disabled}>
  <Story of={CheckboxStories.Disabled} />
</Canvas>

## Indeterminate

Set checkbox to indeterminate state using `indeterminate` prop

<Canvas of={CheckboxStories.Indeterminate}>
  <Story of={CheckboxStories.Indeterminate} />
</Canvas>

## Invalid and feedback

Each `sgds-checkbox` component controls its own `invalid` state.
To enable SGDS form validation styles set `hasFeedback` to true. 

<Canvas of={CheckboxStories.Invalid}>
  <Story of={CheckboxStories.Invalid} />
</Canvas>

When `sgds-checkbox-group` hasFeedback is set to true, it takes precedence and sets all its of its `sgds-checkbox` children `hasFeedback` prop to true 

<Canvas of={CheckboxStories.InvalidGroup}>
  <Story of={CheckboxStories.InvalidGroup} />
</Canvas>


## Validation

### CheckboxGroup 

HTML form constraint validation is built-in. By default, there is no need to control the `invalid` prop to enable constraint validations. 

- To enable invalid styles and feedback message, set `hasFeedback` to true on `sgds-checkbox-group`.

In this example, press submit to show constraint validation behaviour for `required` on the checkboxes. 
Form reset reset's the validity and the value of the input to its initial default value.

  CheckboxGroup constraint validation works as a single form entity. 
  When `required` is set to true on CheckboxGroup, user has to minimally check one option for a valid submission. 
  Do not set `required` on the Checkbox children when using CheckboxGroup.

<Canvas of={CheckboxStories.Validation}>
  <Story of={CheckboxStories.Validation} />
</Canvas>

### Checkbox

Set `hasFeedback="both"` and `required` to true on individual `sgds-checkbox`. Each checkbox will have its own invalid feedback message

<Canvas of={CheckboxStories.ValidationSingle}>
  <Story of={CheckboxStories.ValidationSingle} />
</Canvas>

### Overriding the invalid feedback message 

By default, the invalid feedback reflected is based on the browser's native error message based on the ValidityState.  To override the default message, 
use `invalidFeedback` prop. 

<Canvas of={CheckboxStories.OverrideInvalidFeedback}>
  <Story of={CheckboxStories.OverrideInvalidFeedback} />
</Canvas>