## Sizes

### Small modal

<Canvas of={ModalStories.SmallSize}>
  <Story of={ModalStories.SmallSize} />
</Canvas>

### Medium modal (default)

<Canvas of={ModalStories.MediumSize}>
  <Story of={ModalStories.MediumSize} />
</Canvas>

### Large modal

<Canvas of={ModalStories.LargeSize}>
  <Story of={ModalStories.LargeSize} />
</Canvas>

### Extra large modal

<Canvas of={ModalStories.ExtraLargeSize}>
  <Story of={ModalStories.ExtraLargeSize} />
</Canvas>

### Fullscreen modal

<Canvas of={ModalStories.FullscreenSize}>
  <Story of={ModalStories.FullscreenSize} />
</Canvas>


## Long content 

When content is long, auto-scrolling will be activated in the modal body. Modal header and footer are in fixed position

<Canvas of={ModalStories.LongContent}>
  <Story of={ModalStories.LongContent} />
</Canvas>

## No animation

Disabled animation by setting `noAnimation` to true

<Canvas of={ModalStories.noAnimation}>
  <Story of={ModalStories.noAnimation} />
</Canvas>

## Prevent close

The `sgds-close` event is cancelable, allowing you to prevent the modal from closing by calling `event.preventDefault()`. This is useful when you need to validate user input or confirm an action before allowing the modal to close.

The event provides `event.detail.source` which indicates how the close was triggered:
- `'close-button'` - User clicked the close button
- `'overlay'` - User clicked the modal overlay
- `'keyboard'` - User pressed the Escape key

<Canvas of={ModalStories.PreventClose}>
  <Story of={ModalStories.PreventClose} />
</Canvas>
